/* eslint-env node, mocha */
import {
  expect,
} from 'chai';
import supertest from 'supertest';
import server from '../index';

describe('user controller', () => {
  describe('it should validate user input', () => {
    'it should return a 400 if user inputs wrong details', (done);
  });
});
